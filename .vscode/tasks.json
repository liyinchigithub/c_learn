{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "gcc build",
            "type": "shell",
            "command": "/usr/local/bin/gcc",
            "args": [
                "-g",
                "${workspaceFolder}/main.c",
                "-o",
                "${workspaceFolder}/main"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}